# Copy to ".env" in this folder (never commit .env).

# Gmail SMTP — app password: 16 characters, NO spaces
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=demo329032@gmail.com
SMTP_PASSWORD=

# If port 465 is blocked, try:
# SMTP_PORT=587
# SMTP_USE_STARTTLS=1

ALERT_FROM_EMAIL=demo329032@gmail.com

# AUTH_SECRET=change-me-in-production
